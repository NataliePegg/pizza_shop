# Pizza Sales 

# use rproject to create hard link to the sales folder 
sales_data <- read.csv("sales/202003_sales.csv")

# notice that there is a repository so new git folder
# question marks symbolized un-tracked changes
# click on path and click "commit" button, then add comment